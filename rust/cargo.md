## Cargo

### Commands

- `watch` executes another command on file save
- `fmt` is the official Rust formatter
- `clippy` is the official Rust linter
- `audit` checks dependencies for vulnerabilities
- `expand` expands all macros, shows what the compiler sees
