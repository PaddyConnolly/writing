## Macros

Macros are used for code generation, they take in a stream of symboks (tokens) and output a stream of symbols, which then get passed on to the compiler. We can view the inner workings of a macro with `cargo expand`.
